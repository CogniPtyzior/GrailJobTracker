monolog:
    handlers:
        main:
            type: stream
            path: php://stderr
            level: info
            channels: ["!event", "!doctrine", "!console", "!request", "!security", "!messenger"]
